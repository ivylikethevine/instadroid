# Nothing ignored: app/Dockerfile installs no apt packages (only `apt-get upgrade`; adb comes from the
# adbutils wheel), so DL3008's version-pinning rule has nothing to flag. Give any future ignore a reason.
ignored: []
